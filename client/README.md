# StudySync Client

This is a minimal React + Vite client showing a whiteboard that syncs via Socket.io.

Run locally:

```
cd client
npm install
npm run dev
```

Open two browser windows at the same URL to test drawing sync.
